import { Stack } from "@chakra-ui/react";

import { Header } from "components/Header";

const AboutPage = () => {
    return (
        <Stack>
            <Header showNavbar />
        </Stack>
    );
};

export default AboutPage;
