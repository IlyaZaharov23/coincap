import { bluePrimary, white } from "shared/constants/colors";

export const styles = {
    wrapper: {
        background: white,
        padding: "8px",
        border: `1px solid ${bluePrimary}`,
        borderRadius: "10px",
    },
};
